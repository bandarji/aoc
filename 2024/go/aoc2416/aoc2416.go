package aoc2416

import (
	"fmt"
	"time"
)

const AOC2416_TEST string = ``

func Aoc241601(input string) int {
	start := time.Now()
	defer func() { fmt.Println(time.Now().Sub(start)) }()
	return 0
}

func Aoc241602(input string) int {
	start := time.Now()
	defer func() { fmt.Println(time.Now().Sub(start)) }()
	return 0
}
